# Keep preset mode for the fully offline competition demo.
COACH_MODE=preset

# Required only when COACH_MODE=real. Never prefix these with NEXT_PUBLIC_.
AI_API_KEY=
AI_BASE_URL=https://api.example.com/v1
AI_MODEL=
